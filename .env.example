DATABASE_URL="postgresql://user:password@host:port/database"
PORT=3000

# Keycloak Configuration
KEYCLOAK_URL="http://localhost:8080/auth"
KEYCLOAK_REALM="your-realm"
KEYCLOAK_CLIENT="your-client-id"
KEYCLOAK_CLIENT_SECRET="your-keycloak-client-secret" # Sensitive

# Google Cloud Storage Configuration
GCS_BUCKET_NAME="your-gcs-bucket-name"
GCS_PROJECT_ID="your-gcs-project-id"
GCS_SA_KEY="your-gcs-service-account-key-json-base64" # Sensitive: Base64 encoded JSON key file

# OpenTelemetry Configuration for Grafana Integration
# These variables configure the OpenTelemetry SDK in the Node.js application
# to export telemetry data (traces, metrics, logs) to an OpenTelemetry Collector,
# which can then forward it to Grafana-compatible backends (e.g., Prometheus, Loki, Tempo).

# Specifies the logical name of the service. This name will appear in Grafana dashboards.
# It's crucial for identifying your service's telemetry data.
SERVICE_NAME="medical-registry-api"

# Specifies the endpoint of the OpenTelemetry Collector (or directly to an OTLP-compatible receiver).
# The default OTLP gRPC port is 4317. If using HTTP, it's typically 4318.
# Replace 'localhost:4317' with the actual address of your OpenTelemetry Collector.
OTEL_EXPORTER_OTLP_ENDPOINT="http://localhost:4317"

# Specifies the protocol to use for OTLP exports. Common values are 'grpc' or 'http/protobuf'.
# If your collector is configured for gRPC, use 'grpc'.
OTEL_EXPORTER_OTLP_PROTOCOL="grpc"

# Defines a list of key-value pairs that describe the resource (e.g., application, host, container)
# that emitted the telemetry data. These attributes provide valuable context in Grafana.
# Example: "deployment.environment=production,host.name=my-server,service.version=1.0.0"
OTEL_RESOURCE_ATTRIBUTES="deployment.environment=development,service.version=1.0.0"

# Optional: Set the log level for the OpenTelemetry SDK itself.
# Values can be 'error', 'warn', 'info', 'debug', 'verbose'.
OTEL_LOG_LEVEL="info"

# Optional: Enable or disable specific telemetry signals.
# Set to 'otlp' to export via OTLP, or 'none' to disable.
OTEL_TRACES_EXPORTER="otlp" # Exports traces via OTLP
OTEL_METRICS_EXPORTER="otlp" # Exports metrics via OTLP
OTEL_LOGS_EXPORTER="otlp" # Exports logs via OTLP

